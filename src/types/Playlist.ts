export type Playlist = {
    id: string
    name: string
    description: string
    images: 
    [
        {
            url: string
            width: number
            height: number
        }
    ]
}